# Flask 應用程式設定
SECRET_KEY=your-secret-key-here

# SQL Server 資料庫連線設定
# 格式: mssql+pyodbc://username:password@server/database?driver=ODBC+Driver+17+for+SQL+Server
DATABASE_URL=mssql+pyodbc://username:password@your-server/your-database?driver=ODBC+Driver+17+for+SQL+Server

# 如果使用 SQL Server 認證，格式如下：
# DATABASE_URL=mssql+pyodbc://username:password@your-server/your-database?driver=ODBC+Driver+17+for+SQL+Server

# 如果使用 Windows 認證，格式如下：
# DATABASE_URL=mssql+pyodbc://@your-server/your-database?driver=ODBC+Driver+17+for+SQL+Server&trusted_connection=yes

# 備用 SQLite 資料庫（如果沒有設定 SQL Server）
# DATABASE_URL=sqlite:///checkin.db
